\ProvidesPackage{styles/file}

\newcommand{\IfFileNotEmpty}[2]{%
    \IfFileExists{#1}{%
        \newread\tempfile
        \openin\tempfile=#1
        \read\tempfile to \fileline
        \closein\tempfile
        \ifeof\tempfile

        \else
            #2
        \fi
    }{}%
}